apiVersion: v1
kind: Service
metadata:
  name: nelcaso-core
  namespace: nelcaso
  labels:
    app.kubernetes.io/name: nelcaso-core
    app.kubernetes.io/instance: nelcaso-core
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: nelcaso-core
    app.kubernetes.io/instance: nelcaso-core
